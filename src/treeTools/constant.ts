/** 默认的树节点 parentId、id、children 的字段 */
export const DEFAULT_FIELD_NAMES = {
  /** 唯一标识属性名 */
  id: 'id',
  /** 子节点集合属性名 */
  children: 'children',
  /** 父节点标识属性名 */
  parentId: 'parentId',
} as const
