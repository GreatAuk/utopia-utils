/**
 * @vitest-environment happy-dom
 */
import { describe, it } from 'vitest'

// import { toBase64 } from './toBase64'

describe('toBase64', () => {
  it('should convert a File or Blob or imgUrl to a base64 encoded string', async () => {
    // const file = new File(['hello world'], 'hello.txt', { type: 'text/plain' })
    // const blob = new Blob(['hello world'], { type: 'text/plain' })
    // const imgUrl = 'data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQABAAD/2wCEAAkGBxQTEhQUExQWFhQYGBgYGBgYGBgYGBgYGBgYGBgYGBgYHSggGBolGxgXITEhJSkrLi4uGB8zODMsNygtLisB'

    // const base64 = 'data:text/plain;base64,aGVsbG8gd29ybGQ='

    // expect(await toBase64(file)).toBe(base64)
    // expect(await toBase64(blob)).toBe(base64)
    // expect(await toBase64(imgUrl)).toBe(imgUrl)
  })
})
